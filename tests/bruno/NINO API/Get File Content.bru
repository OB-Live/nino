meta {
  name: Get File Content
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/file/:filepath
  body: none
  auth: inherit
}

params:path {
  filepath: petstore/tables.yaml
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

example {
  name: 200 Response
  description: The raw content of the file.
  
  request: {
    url: {{baseUrl}}/api/file/:filepath
    method: GET
    mode: none
    params:path: {
      filepath: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
