meta {
  name: Execute Pimo
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/pimo/exec
  body: json
  auth: inherit
}

body:json {
  {
    "yaml": "petstore/pets-masking.yaml",
    "json": "{\"name\": \"John Doe\"}"
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

example {
  name: 200 Response
  description: The masked output from the pimo command.
  
  request: {
    url: {{baseUrl}}/api/pimo/exec
    method: POST
    mode: json
    body:json: {
      {
        "yaml": "",
        "json": ""
      }
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
