meta {
  name: Get Project Schema
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/schema.{{format}}
  body: none
  auth: inherit
}

params:path {
  format: dot
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
 
example {
  name: 200 Response
  description: Schema graph in the specified format.
  
  request: {
    url: {{baseUrl}}/api/schema.:format
    method: GET
    mode: none
    params:path: {
      format: 
    }
  }
  
  response: {
    headers: {
      Content-Type: text/vnd.graphviz
    }
  
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}

example {
  name: 200 Response
  description: Schema graph in the specified format.
  
  request: {
    url: {{baseUrl}}/api/schema.:format
    method: GET
    mode: none
    params:path: {
      format: 
    }
  }
  
  response: {
    headers: {
      Content-Type: image/svg+xml
    }
  
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: xml
      content: '''
  
      '''
    }
  }
}

example {
  name: 200 Response
  description: Schema graph in the specified format.
  
  request: {
    url: {{baseUrl}}/api/schema.:format
    method: GET
    mode: none
    params:path: {
      format: 
    }
  }
  
  response: {
    headers: {
      Content-Type: image/png
    }
  
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
