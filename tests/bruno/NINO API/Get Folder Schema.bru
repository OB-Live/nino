meta {
  name: Get Folder Schema
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/schema/:folder.:format
  body: none
  auth: inherit
}

params:path {
  folder: 
  format: 
}

example {
  name: 200 Response
  description: Schema graph for the specified folder.
  
  request: {
    url: {{baseUrl}}/api/schema/:folder.:format
    method: GET
    mode: none
    params:path: {
      folder: 
      format: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
