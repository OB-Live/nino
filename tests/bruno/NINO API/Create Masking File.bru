meta {
  name: Create Masking File
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/new/mask/:folderName/:tableName
  body: none
  auth: inherit
}

params:path {
  folderName: petstore
  tableName: pets
}

tests {
  test("Status code is 201", function() {
    expect(res.getStatus()).to.equal(201);
  });
}

example {
  name: 201 Response
  description: Masking file created successfully.
  
  request: {
    url: {{baseUrl}}/api/new/mask/:folderName/:tableName
    method: GET
    mode: none
    params:path: {
      folderName: 
      tableName: 
    }
  }
  
  response: {
    status: {
      code: 201
      text: Created
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
